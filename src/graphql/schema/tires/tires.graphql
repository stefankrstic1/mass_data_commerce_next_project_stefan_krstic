query GetAllTires {
  tires {
    id
    brand
    model
    price
    rating
    size
    stock
    type
    image
  }
}

query GetTireById($tireId: Float!) {
  tire(id: $tireId) {
    id
    brand
    model
    price
    rating
    size
    stock
    type
  }
}
